import styled from 'styled-components';

export default styled.h1`
  margin: 0;
  padding: 1rem 0;
  background: -webkit-linear-gradient(var(--color-white), var(--color-blond));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
`;
